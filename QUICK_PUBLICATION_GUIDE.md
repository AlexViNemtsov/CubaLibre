# 🚀 Быстрая инструкция по публикации в Telegram

## ✅ Что уже готово:

1. ✅ Privacy Policy создана и доступна
2. ✅ Футер с ссылкой на Privacy Policy добавлен
3. ✅ Бот настроен и готов
4. ✅ Все функции реализованы
5. ✅ Безопасность настроена

## 📋 Что нужно сделать для публикации:

### 1. Деплой на сервер (обязательно)

**Вариант A: Render.com (рекомендуется)**
```bash
# 1. Создайте аккаунт на render.com
# 2. Подключите ваш Git репозиторий
# 3. Создайте новый Web Service
# 4. Используйте render.yaml из проекта
# 5. Добавьте переменные окружения в панели Render
```

**Вариант B: Другой хостинг**
- Загрузите код на сервер
- Настройте PostgreSQL базу данных
- Установите зависимости: `npm install --production`
- Соберите frontend: `cd frontend && npm run build`
- Настройте .env файл
- Запустите сервер: `npm start` (или через PM2)

### 2. Настройка HTTPS (обязательно!)

Telegram требует HTTPS. Варианты:

**A. Let's Encrypt (бесплатно)**
```bash
# Установите certbot
sudo apt-get install certbot
# Получите сертификат
sudo certbot certonly --standalone -d ваш-домен.com
```

**B. Используйте хостинг с автоматическим HTTPS** (Render, Vercel, Railway и т.д.)

### 3. Настройка переменных окружения (.env)

Создайте `.env` на сервере:
```env
NODE_ENV=production
TELEGRAM_BOT_TOKEN=ваш_токен_бота
DB_HOST=ваш_хост_бд
DB_PORT=5432
DB_NAME=cuba_clasificados
DB_USER=ваш_пользователь
DB_PASSWORD=ваш_пароль
PORT=3000
FRONTEND_URL=https://ваш-домен.com
WEB_APP_URL=https://ваш-домен.com
UPLOAD_DIR=./uploads
```

### 4. Настройка Web App в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/myapps`
3. Выберите вашего бота
4. Нажмите "Web App" → "Edit"
5. Введите **Web App URL**: `https://ваш-домен.com` (обязательно HTTPS!)
6. Сохраните

### 5. Сборка и запуск

```bash
# 1. Соберите frontend
cd frontend
npm run build

# 2. Вернитесь в корень
cd ..

# 3. Запустите сервер (или через PM2)
npm start

# Или через PM2 (рекомендуется для production):
pm2 start server/index.js --name cuba-clasificados
pm2 save
pm2 startup
```

### 6. Проверка работы

1. **Проверьте API:**
   - Откройте: `https://ваш-домен.com/api/health`
   - Должен вернуть: `{"status":"ok",...}`

2. **Проверьте Frontend:**
   - Откройте: `https://ваш-домен.com`
   - Должна загрузиться главная страница

3. **Проверьте Privacy Policy:**
   - Откройте: `https://ваш-домен.com/privacy-policy.html`
   - Должна загрузиться страница

4. **Проверьте в Telegram:**
   - Откройте бота в Telegram
   - Отправьте `/start`
   - Нажмите кнопку "📱 Abrir aplicación"
   - Приложение должно открыться без ошибок

## ⚠️ Критически важно:

1. **HTTPS обязателен** - без него Telegram не будет работать
2. **Домен должен быть валидным** - не используйте localhost или IP
3. **Privacy Policy должна быть доступна** - проверьте ссылку
4. **Все переменные окружения настроены** - особенно `WEB_APP_URL`

## 🔍 Если что-то не работает:

1. **Проверьте логи сервера** - там будут ошибки
2. **Проверьте консоль браузера** (F12) - там могут быть ошибки загрузки
3. **Проверьте, что бот запущен** - `node bot/index.js` или через PM2
4. **Проверьте базу данных** - подключение и миграции
5. **Проверьте HTTPS** - сертификат должен быть валидным

## 📝 Полный чеклист:

Смотрите файл `PUBLICATION_CHECKLIST.md` для детального списка всех пунктов.

---

**Готово!** После выполнения всех шагов ваше приложение будет доступно в Telegram! 🎉
