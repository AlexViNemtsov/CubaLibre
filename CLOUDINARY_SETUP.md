# 📸 Настройка Cloudinary для хранения изображений

## Проблема
На Render файловая система **эфемерная** - при каждом перезапуске или деплое все файлы в папке `./uploads` удаляются. Это приводит к тому, что загруженные изображения пропадают.

## Решение: Cloudinary
Cloudinary - бесплатный сервис для хранения и оптимизации изображений.

### Шаг 1: Регистрация в Cloudinary

1. Зайди на [cloudinary.com](https://cloudinary.com)
2. Зарегистрируйся (бесплатный план даёт 25 GB хранилища и 25 GB трафика в месяц)
3. После регистрации зайди в Dashboard

### Шаг 2: Получение ключей API

В Dashboard Cloudinary найди:
- **Cloud name** (например: `d123456789`)
- **API Key** (например: `123456789012345`)
- **API Secret** (например: `abcdefghijklmnopqrstuvwxyz123456`)

### Шаг 3: Настройка переменных окружения на Render

Добавь в переменные окружения на Render:

```
CLOUDINARY_CLOUD_NAME=твой_cloud_name
CLOUDINARY_API_KEY=твой_api_key
CLOUDINARY_API_SECRET=твой_api_secret
```

### Шаг 4: Деплой

После добавления переменных окружения:
1. Render автоматически перезапустит сервис
2. Сервер определит, что Cloudinary настроен, и начнёт использовать его
3. Все новые загруженные изображения будут сохраняться в Cloudinary

### Как это работает

- **Если Cloudinary настроен**: изображения загружаются в Cloudinary и возвращаются полные URL (например: `https://res.cloudinary.com/...`)
- **Если Cloudinary не настроен**: используется локальное хранилище (как раньше, но файлы будут пропадать при перезапуске)

### Миграция существующих изображений

Существующие изображения в БД с путями `/uploads/...` продолжат работать, если они ещё доступны. Но для новых загрузок будет использоваться Cloudinary.

### Преимущества Cloudinary

✅ Файлы не пропадают при перезапуске сервера
✅ Автоматическая оптимизация изображений
✅ CDN для быстрой загрузки по всему миру
✅ Бесплатный план: 25 GB хранилища и 25 GB трафика в месяц
✅ Автоматическое создание миниатюр и ресайз
