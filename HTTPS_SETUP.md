# 🔒 Настройка HTTPS для Telegram Web App

Telegram требует HTTPS для Web Apps. Для локальной разработки используйте туннель.

## Вариант 1: ngrok (рекомендуется)

### Установка:
```bash
# macOS
brew install ngrok/ngrok/ngrok

# Или скачайте с https://ngrok.com/download
```

### Использование:
```bash
# Запустите туннель на порт 5173
ngrok http 5173
```

Вы получите HTTPS URL вида: `https://xxxx-xx-xx-xx-xx.ngrok-free.app`

**Используйте этот URL в BotFather как Web App URL!**

### Автоматический запуск:
```bash
./setup-https.sh
```

---

## Вариант 2: cloudflared (бесплатно, без регистрации)

### Установка:
```bash
brew install cloudflared
```

### Использование:
```bash
cloudflared tunnel --url http://localhost:5173
```

Вы получите HTTPS URL вида: `https://xxxx-xx-xx-xx-xx.trycloudflare.com`

**Используйте этот URL в BotFather как Web App URL!**

---

## Вариант 3: Локальный HTTPS сервер (сложнее)

Можно настроить локальный HTTPS с самоподписанным сертификатом, но Telegram может его не принять.

---

## Пошаговая инструкция:

1. **Установите ngrok или cloudflared** (см. выше)

2. **Убедитесь, что frontend запущен:**
   ```bash
   cd frontend && npm run dev
   ```

3. **Запустите туннель:**
   ```bash
   # ngrok
   ngrok http 5173
   
   # или cloudflared
   cloudflared tunnel --url http://localhost:5173
   ```

4. **Скопируйте HTTPS URL** (например: `https://abc123.ngrok-free.app`)

5. **Обновите Web App в BotFather:**
   - Откройте [@BotFather](https://t.me/BotFather)
   - `/myapps` → выберите вашего бота
   - Выберите Web App → Edit
   - Вставьте HTTPS URL

6. **Готово!** Теперь можно открыть бота в Telegram

---

## ⚠️ Важно:

- **Туннель должен быть запущен** пока вы используете бота
- При каждом перезапуске ngrok/cloudflared URL может измениться
- Для production используйте реальный домен с SSL сертификатом

---

## Быстрый старт:

```bash
# 1. Установите ngrok (если еще не установлен)
brew install ngrok/ngrok/ngrok

# 2. Запустите туннель
ngrok http 5173

# 3. Скопируйте HTTPS URL и используйте в BotFather
```




