# 🚀 Инструкция по запуску бота и созданию тестового объявления

## Шаг 1: Установка зависимостей

```bash
# Установка backend зависимостей
npm install

# Установка frontend зависимостей
cd frontend && npm install && cd ..
```

## Шаг 2: Создание .env файла

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cuba_clasificados
DB_USER=postgres
DB_PASSWORD=postgres
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173
UPLOAD_DIR=./uploads
WEB_APP_URL=http://localhost:5173
```

## Шаг 3: Создание базы данных

```bash
# Создать базу данных PostgreSQL
createdb cuba_clasificados
```

Если у вас нет PostgreSQL локально, можно использовать Supabase (бесплатно):
1. Зарегистрируйтесь на https://supabase.com
2. Создайте новый проект
3. Скопируйте connection string и обновите .env

## Шаг 4: Запуск проекта

Откройте **3 терминала**:

### Терминал 1 - Backend сервер:
```bash
npm run dev
```

### Терминал 2 - Frontend:
```bash
cd frontend
npm run dev
```

### Терминал 3 - Telegram Bot (опционально):
```bash
node bot/index.js
```

## Шаг 5: Настройка Telegram Bot

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newapp`
3. Выберите вашего бота (или создайте нового через `/newbot`)
4. Заполните:
   - **Название**: Cuba Clasificados
   - **Описание**: Доска объявлений для Кубы
   - **Web App URL**: `http://localhost:5173` (для разработки)

## Шаг 6: Создание тестового объявления

### Вариант A: Через интерфейс (рекомендуется)

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку "Открыть приложение"
4. Выберите город (La Habana)
5. Выберите категорию (например, Аренда)
6. Нажмите кнопку "+" для создания объявления
7. Заполните форму и опубликуйте

### Вариант B: Через API (для тестирования)

Используйте скрипт `create-test-listing.js`:

```bash
node create-test-listing.js
```

Или через curl:

```bash
curl -X POST http://localhost:3000/api/listings \
  -H "Content-Type: application/json" \
  -d '{
    "category": "rent",
    "scope": "NEIGHBORHOOD",
    "city": "La Habana",
    "neighborhood": "Vedado",
    "title": "Квартира в центре",
    "description": "Уютная квартира в центре Vedado",
    "price": "50000",
    "currency": "CUP",
    "rent_type": "apartment",
    "rent_period": "monthly",
    "is_available_now": true,
    "landmark": "Рядом с университетом",
    "contact_telegram": "@testuser"
  }'
```

## Проверка работы

1. Откройте http://localhost:3000/api/health - должен вернуть `{"status":"ok"}`
2. Откройте http://localhost:3000/api/listings - должен вернуть список объявлений
3. Откройте бота в Telegram и проверьте работу Web App

## Решение проблем

### Ошибка подключения к БД
- Убедитесь, что PostgreSQL запущен: `pg_isready`
- Проверьте credentials в `.env`
- Убедитесь, что база данных создана

### Порт уже занят
- Измените PORT в `.env`
- Или остановите процесс на порту 3000/5173

### Frontend не подключается к API
- Проверьте, что backend запущен на порту 3000
- Проверьте CORS настройки в `server/index.js`




