# 🚀 Локальный запуск проекта

## Быстрый старт (3 команды)

### 1. Установка зависимостей
```bash
npm install
cd frontend && npm install && cd ..
```

### 2. Запуск (2 терминала)

**Терминал 1 - Backend:**
```bash
npm run dev
```
Backend запустится на http://localhost:3000

**Терминал 2 - Frontend:**
```bash
cd frontend && npm run dev
```
Frontend запустится на http://localhost:5173

### 3. Проверка

Открой в браузере: http://localhost:5173

Должен отображаться текст: **"Cuba Clasificados - plataforma gratuita única para Cuba"**

## Настройка Telegram Bot для локального тестирования

1. Открой [@BotFather](https://t.me/BotFather)
2. Отправь `/newapp`
3. Выбери своего бота
4. Укажи Web App URL: `http://localhost:5173` (для разработки)

⚠️ **Важно:** Для локального тестирования используй ngrok или другой туннель для HTTPS:
```bash
# Установи ngrok
brew install ngrok

# Запусти туннель
ngrok http 5173

# Используй HTTPS URL из ngrok в BotFather
```

## Что дальше?

1. ✅ Протестируй всё локально
2. ✅ Убедись, что текст правильный
3. ✅ Проверь создание объявлений
4. ✅ Потом залей на reg.ru и GitHub

## Структура проекта

- `frontend/` - React приложение (Vite)
- `server/` - Express API сервер
- `bot/` - Telegram бот
- `.env` - Настройки (создай из `.env.example`)
