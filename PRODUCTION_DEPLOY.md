# 🚀 Деплой на продакшн

## Архитектура

- **Frontend**: reg.ru (статический хостинг)
- **Backend**: Render.com (Node.js сервер)
- **База данных**: PostgreSQL (Render или внешняя)
- **Бот**: Запускается на Render вместе с backend

## 📋 Чеклист перед деплоем

### 1. Подготовка кода

- [ ] Все изменения закоммичены в Git
- [ ] Код протестирован локально
- [ ] Переменные окружения подготовлены
- [ ] Миграции БД готовы

### 2. Настройка переменных окружения

Создайте файл `.env.production` для справки (не коммитьте его!):

```env
# Backend (Render)
NODE_ENV=production
PORT=3000
TELEGRAM_BOT_TOKEN=ваш_токен_бота
REQUIRED_CHANNEL=@CubaClasificados

# База данных
DB_HOST=ваш_хост_бд
DB_PORT=5432
DB_NAME=cuba_clasificados
DB_USER=ваш_пользователь
DB_PASSWORD=ваш_пароль

# URLs
FRONTEND_URL=https://ваш-домен-reg.ru
WEB_APP_URL=https://ваш-домен-reg.ru
UPLOAD_DIR=./uploads

# Для frontend (в vite.config.js или при сборке)
VITE_API_URL=https://ваш-backend.onrender.com/api
```

---

## 🔧 Деплой Backend на Render

### Шаг 1: Подготовка репозитория

1. Убедитесь, что код в GitHub:
   ```bash
   git add .
   git commit -m "Prepare for production deployment"
   git push origin main
   ```

### Шаг 2: Создание сервиса на Render

1. Зайдите на [render.com](https://render.com)
2. Нажмите "New" → "Web Service"
3. Подключите ваш GitHub репозиторий
4. Выберите репозиторий и ветку (обычно `main`)

### Шаг 3: Настройка сервиса

**Build Command:**
```bash
npm install
```

**Start Command:**
```bash
npm start
```

**Environment:**
- Выберите `Node`

### Шаг 4: Переменные окружения на Render

В настройках сервиса добавьте все переменные из `.env.production`:

```
NODE_ENV=production
PORT=3000
TELEGRAM_BOT_TOKEN=ваш_токен
REQUIRED_CHANNEL=@CubaClasificados
DB_HOST=ваш_хост
DB_PORT=5432
DB_NAME=cuba_clasificados
DB_USER=ваш_пользователь
DB_PASSWORD=ваш_пароль
FRONTEND_URL=https://ваш-домен-reg.ru
WEB_APP_URL=https://ваш-домен-reg.ru
UPLOAD_DIR=./uploads
```

### Шаг 5: База данных

**Вариант A: Render PostgreSQL (рекомендуется)**
1. Создайте PostgreSQL базу данных на Render
2. Скопируйте connection string
3. Используйте его в переменных окружения

**Вариант B: Внешняя БД**
1. Используйте существующую PostgreSQL БД
2. Укажите хост, порт, имя, пользователя, пароль

### Шаг 6: Деплой

1. Нажмите "Create Web Service"
2. Render автоматически задеплоит приложение
3. Дождитесь завершения деплоя
4. Скопируйте URL сервиса (например: `https://cuba-clasificados.onrender.com`)

### Шаг 7: Проверка Backend

Проверьте, что backend работает:
- Health check: `https://ваш-backend.onrender.com/api/health`
- Должен вернуть: `{"status":"ok",...}`

---

## 🎨 Деплой Frontend на reg.ru

### Шаг 1: Сборка Frontend

**Важно:** Перед сборкой обновите `vite.config.js` с правильным API URL!

1. Откройте `frontend/vite.config.js`
2. Убедитесь, что API URL указывает на ваш Render backend:
   ```js
   define: {
     'import.meta.env.VITE_API_URL': JSON.stringify(
       process.env.VITE_API_URL || 'https://ваш-backend.onrender.com/api'
     )
   }
   ```

3. Соберите frontend:
   ```bash
   cd frontend
   npm run build
   ```

4. Проверьте, что `frontend/dist/` содержит собранные файлы

### Шаг 2: Загрузка на reg.ru

**Через FTP:**
1. Подключитесь к FTP reg.ru
2. Загрузите **все содержимое** папки `frontend/dist/` в корень сайта (обычно `public_html/` или `www/`)
3. Убедитесь, что `index.html` в корне

**Через панель управления:**
1. Зайдите в панель управления reg.ru
2. Откройте файловый менеджер
3. Загрузите файлы из `frontend/dist/`

### Шаг 3: Проверка Frontend

1. Откройте `https://ваш-домен-reg.ru`
2. Должна загрузиться главная страница
3. Проверьте консоль браузера (F12) - не должно быть ошибок загрузки API

### Шаг 4: Настройка HTTPS (если нужно)

Если reg.ru не предоставляет автоматический HTTPS:
1. Установите SSL сертификат (Let's Encrypt)
2. Или используйте встроенный SSL от reg.ru

---

## 🤖 Настройка бота

### Шаг 1: Запуск бота на Render

Бот запускается автоматически вместе с backend, так как `bot/index.js` экспортируется.

**Или создайте отдельный сервис для бота:**

1. На Render создайте новый "Background Worker"
2. Подключите тот же репозиторий
3. Start Command: `node bot/index.js`
4. Добавьте те же переменные окружения

### Шаг 2: Настройка Web App в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/myapps`
3. Выберите вашего бота
4. Нажмите "Web App" → "Edit"
5. Укажите **Web App URL**: `https://ваш-домен-reg.ru` (HTTPS обязательно!)
6. Сохраните

---

## ✅ Проверка после деплоя

### 1. Backend проверки

- [ ] Health check работает: `https://ваш-backend.onrender.com/api/health`
- [ ] API возвращает данные: `https://ваш-backend.onrender.com/api/listings`
- [ ] Загрузка изображений работает
- [ ] База данных подключена (проверьте логи на Render)

### 2. Frontend проверки

- [ ] Главная страница загружается: `https://ваш-домен-reg.ru`
- [ ] Privacy Policy доступна: `https://ваш-домен-reg.ru/privacy-policy.html`
- [ ] API запросы идут на правильный backend
- [ ] Нет ошибок в консоли браузера (F12)

### 3. Бот проверки

- [ ] Бот отвечает на `/start`
- [ ] Проверка подписки на канал работает
- [ ] Кнопка "Abrir aplicación" открывает Web App
- [ ] Web App загружается без ошибок

### 4. Функциональные проверки

- [ ] Создание объявления работает
- [ ] Просмотр объявлений работает
- [ ] Редактирование работает
- [ ] Удаление работает
- [ ] Загрузка фотографий работает
- [ ] Счетчик просмотров работает

---

## 🔧 Настройка CORS

Если возникают проблемы с CORS, убедитесь, что в `server/index.js`:

```js
app.use(cors({
  origin: process.env.FRONTEND_URL || 'https://ваш-домен-reg.ru',
  credentials: true
}));
```

---

## 📝 Важные файлы для деплоя

### Backend (Render):
- `server/index.js` - точка входа
- `server/routes/` - API routes
- `server/database/` - миграции БД
- `bot/index.js` - Telegram бот
- `package.json` - зависимости

### Frontend (reg.ru):
- `frontend/dist/index.html` - главная страница
- `frontend/dist/assets/` - JS и CSS файлы
- `frontend/dist/images/` - изображения
- `frontend/dist/privacy-policy.html` - Privacy Policy

---

## 🐛 Решение проблем

### Проблема: Frontend не может подключиться к API

**Решение:**
1. Проверьте `VITE_API_URL` в `vite.config.js`
2. Проверьте CORS настройки на backend
3. Проверьте, что backend доступен по HTTPS

### Проблема: Бот не отвечает

**Решение:**
1. Проверьте логи на Render
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` правильный
3. Проверьте, что бот запущен (логи должны показывать "🤖 Telegram Bot is running...")

### Проблема: База данных не подключается

**Решение:**
1. Проверьте переменные окружения на Render
2. Проверьте, что БД доступна из Render (белый список IP)
3. Проверьте логи подключения

### Проблема: Изображения не загружаются

**Решение:**
1. Убедитесь, что `uploads/` директория существует на Render
2. Проверьте права на запись
3. Проверьте, что `/uploads` маршрут настроен в `server/index.js`

---

## 🔄 Обновление после изменений

### Backend:
1. Закоммитьте изменения в Git
2. Push в GitHub
3. Render автоматически задеплоит изменения

### Frontend:
1. Внесите изменения
2. Соберите: `cd frontend && npm run build`
3. Загрузите `frontend/dist/` на reg.ru

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи на Render (Dashboard → Logs)
2. Проверьте консоль браузера (F12)
3. Проверьте Network tab в браузере для API запросов

---

**Готово!** После выполнения всех шагов ваше приложение будет работать на продакшн! 🎉
