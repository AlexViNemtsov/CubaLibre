# 📢 Настройка проверки подписки на канал

## Описание

Приложение теперь требует подписку на канал `@CubaClasificados` для использования бота и Web App.

## Настройка

### 1. Добавьте переменную окружения

В файле `.env` добавьте:

```env
REQUIRED_CHANNEL=@CubaClasificados
```

Или без @:

```env
REQUIRED_CHANNEL=CubaClasificados
```

### 2. Убедитесь, что бот добавлен в канал

**Важно:** Бот должен быть администратором канала или иметь права на чтение участников!

1. Откройте канал `@CubaClasificados`
2. Перейдите в настройки канала → Администраторы
3. Добавьте вашего бота как администратора (или дайте права на чтение участников)

### 3. Проверка работы

1. Запустите бота: `node bot/index.js`
2. Откройте бота в Telegram
3. Отправьте `/start`
4. Если не подписан - увидите сообщение с кнопкой подписки
5. После подписки - сможете использовать приложение

## Как это работает

### В боте:
- При команде `/start` или `/app` проверяется подписка
- Если не подписан - показывается сообщение с кнопкой подписки
- Кнопка "🔄 Verificar suscripción" позволяет проверить подписку снова

### В Web App:
- При загрузке приложения проверяется подписка через API
- Если не подписан - показывается экран с запросом подписки
- Кнопка "🔄 Ya me suscribí, verificar" позволяет проверить подписку снова

## API Endpoint

Создан endpoint для проверки подписки:

```
POST /api/subscription/check
Body: { "userId": 123456789 }
Response: { "subscribed": true/false, "channel": "@CubaClasificados" }
```

## Режим разработки

В режиме разработки (`NODE_ENV !== 'production'`):
- Если канал не найден или ошибка API - доступ разрешается
- Это позволяет разрабатывать без реального канала

## Важные замечания

1. **Бот должен быть администратором канала** - иначе не сможет проверять подписки
2. **Канал должен существовать** - создайте канал перед настройкой
3. **Права бота** - бот должен иметь право читать список участников

## Изменение канала

Чтобы изменить требуемый канал, просто обновите переменную `REQUIRED_CHANNEL` в `.env`:

```env
REQUIRED_CHANNEL=@OtroCanal
```

И перезапустите бота и сервер.
