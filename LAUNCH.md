# 🚀 БЫСТРЫЙ ЗАПУСК БОТА И СОЗДАНИЕ ТЕСТОВОГО ОБЪЯВЛЕНИЯ

## Вариант 1: Автоматический запуск (рекомендуется)

```bash
# 1. Запустите скрипт подготовки
./run.sh

# 2. Откройте 2 терминала и запустите:

# Терминал 1 - Backend:
npm run dev

# Терминал 2 - Frontend:
cd frontend && npm run dev
```

## Вариант 2: Ручной запуск

### Шаг 1: Установка зависимостей (если еще не установлены)

```bash
npm install
cd frontend && npm install && cd ..
```

### Шаг 2: Создание .env файла

```bash
cat > .env << 'EOF'
TELEGRAM_BOT_TOKEN=8418701976:AAHReaTPs92VXuc8pofZSKo_Q_K-4GDCQhY
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cuba_clasificados
DB_USER=postgres
DB_PASSWORD=postgres
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173
UPLOAD_DIR=./uploads
WEB_APP_URL=http://localhost:5173
EOF
```

### Шаг 3: Создание базы данных

```bash
createdb cuba_clasificados
```

### Шаг 4: Запуск серверов

**Терминал 1 - Backend:**
```bash
npm run dev
```

**Терминал 2 - Frontend:**
```bash
cd frontend && npm run dev
```

### Шаг 5: Создание тестового объявления

**Терминал 3:**
```bash
node test-api.js
```

Это создаст тестовое объявление напрямую в базе данных.

## Проверка работы

1. **Проверка API:**
   ```bash
   curl http://localhost:3000/api/health
   # Должен вернуть: {"status":"ok",...}
   
   curl http://localhost:3000/api/listings
   # Должен вернуть список объявлений
   ```

2. **Проверка в браузере:**
   - Backend: http://localhost:3000/api/listings
   - Frontend: http://localhost:5173

3. **Проверка в Telegram:**
   - Откройте [@BotFather](https://t.me/BotFather)
   - `/newapp` → выберите бота
   - Web App URL: `http://localhost:5173`
   - Откройте бота → `/start` → "Открыть приложение"

## Создание объявления через интерфейс

1. Откройте бота в Telegram
2. Нажмите `/start`
3. Нажмите "Открыть приложение"
4. Выберите город (La Habana)
5. Выберите категорию (Аренда/Вещи/Услуги)
6. Нажмите кнопку "+" в правом верхнем углу
7. Заполните форму и опубликуйте

## Что делать если что-то не работает

### Ошибка подключения к БД
```bash
# Проверьте, что PostgreSQL запущен
pg_isready

# Проверьте настройки
psql -U postgres -d cuba_clasificados -c "SELECT 1;"
```

### Порт занят
```bash
# Найдите процесс на порту 3000
lsof -i :3000

# Или измените PORT в .env
```

### Frontend не подключается
- Убедитесь, что backend запущен на порту 3000
- Проверьте консоль браузера на ошибки CORS

## Готово! 🎉

Теперь у вас работает:
- ✅ Backend API на http://localhost:3000
- ✅ Frontend на http://localhost:5173
- ✅ Тестовое объявление в базе данных
- ✅ Telegram Bot готов к использованию




