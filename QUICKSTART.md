# 🚀 Быстрый старт

## Минимальная настройка за 5 минут

### 1. Установите зависимости

```bash
npm install
cd frontend && npm install && cd ..
```

### 2. Создайте файл `.env`

Скопируйте `.env.example` в `.env` и заполните:

```env
TELEGRAM_BOT_TOKEN=8418701976:AAHReaTPs92VXuc8pofZSKo_Q_K-4GDCQhY
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cuba_clasificados
DB_USER=postgres
DB_PASSWORD=postgres
PORT=3000
FRONTEND_URL=http://localhost:5173
```

### 3. Создайте базу данных

```bash
createdb cuba_clasificados
```

### 4. Запустите проект

**Терминал 1:**
```bash
npm run dev
```

**Терминал 2:**
```bash
cd frontend && npm run dev
```

### 5. Настройте Telegram Bot

1. Откройте [@BotFather](https://t.me/BotFather)
2. `/newapp` → выберите бота
3. Web App URL: `http://localhost:5173` (для разработки)

### 6. Протестируйте

1. Откройте бота в Telegram
2. `/start`
3. Нажмите "Открыть приложение"
4. Создайте тестовое объявление

## ✅ Готово!

Теперь у вас работает локальная доска объявлений для Кубы!

## 📝 Следующие шаги

- Настройте production окружение (см. README.md)
- Добавьте реальные данные
- Настройте домен и HTTPS для production




